# 🎄 Новогодняя страховая компания (demo)

Шуточное веб-приложение: **«Новогоднее страхование деятельности Деда Мороза»**.

**Важно:** схема `catalog/schema.json` — единственный источник истины. В коде она используется как есть.

## Быстрый старт (локально)

### 1) Скачайте проект и перейдите в папку

```bash
cd santa_insurance_demo
```

### 2) Создайте виртуальное окружение и установите зависимости

```bash
python -m venv .venv
# macOS/Linux:
source .venv/bin/activate
# Windows PowerShell:
# .venv\Scripts\Activate.ps1

pip install -r requirements.txt
```

### 3) Запустите сервер

```bash
uvicorn app.main:app --reload
```

Откройте в браузере:

- UI (мастер): http://127.0.0.1:8000/ui/start
- API catalog: http://127.0.0.1:8000/api/catalog

## Demo flow (end-to-end)

Старт → Условия (3 шага) → Риски → Калькулятор → Контакты → Оплата → Финал → PDF/JSON-LD.

На экране "Оплата" нажмите **Оплатить** — в демо полис станет `PAID`.

## Проверки качества

```bash
ruff check .
black --check .
pytest
```
